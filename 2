#include <stdio.h> //Подключение библиотеки ввода/вывода
#include <string.h>
#include <math.h> //Подключение библиотеки математических функций
#include <locale.h>
 
//Точка входа в программу - главная функция
int main() {
    setlocale(LC_ALL, "Russian");
    
    float pi, d, L;
//Вычисляем диаметра окружности
    printf ("Вычисление диаметра окружности L = pi*d\n");
    printf ("Введите значение диаметра окружности: ");
    scanf ("%f", &d);
    pi = 3.14;
    L = pi*d;
//Вывод результатов
   
    printf("Значение длины окружности %6.2f\n",L);
    
    return 0;
}
